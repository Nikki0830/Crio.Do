//accessing elements in matrix

//To access elements in a matrix, you use two indices: the first for the row and the second for the column.
let matrix = [
    [1,2,3],
    [4,4,6]
];

let element = matrix[0][1]
console.log(element)//output 2